### EXTRACTION TASK INSTRUCTIONS ###

You are an expert computational linguist. You have thoroughly read the LINGUISTIC ANNOTATION GUIDELINES provided above. 

Your task is to analyze the following sentence and extract any words or phrases that function as Engagement markers. 

CRITICAL GUARDRAILS:
1. RESTRICTED LABEL SPACE: You are STRICTLY FORBIDDEN from using any labels other than the following 10 exact strings:
   ["ATTRIBUTION", "PROCLAIM", "COUNTER", "DENY", "ENTERTAIN", "MONOGLOSS", "CITATION", "ENDOPHORIC", "JUSTIFYING", "SOURCES"]
2. LABEL MAPPING: According to the evaluation parameters, you must collapse certain sub-categories:
   - If the guidelines classify a marker as "Concur" or "Pronounce", output "PROCLAIM".
   - If the guidelines classify a marker as "Endorse" or "Attribute", output "ATTRIBUTION".
3. Exact Text: Extract the exact words that trigger the rhetorical move. Do not include adjacent punctuation.
4. Context Anchoring: For every marker, provide the "context_before" string (the 2-3 words immediately preceding the marker). If it is at the beginning of the sentence, leave it empty.
5. No Hallucinations: Do not invent categories like "CONCLUDE" or "STATE_PROBLEM". If no markers from the 10 approved labels exist, return an empty list [].

Analyze the sentence using a <thought_process> block to classify the semantic function, then output the final JSON array.

Sentence to analyze:
"{sentence}"

Output format MUST be strictly JSON:
[
  {
    "label": "THE_APPROVED_LABEL_ONLY",
    "text": "the exact marker text",
    "context_before": "the preceding words"
  }
]
