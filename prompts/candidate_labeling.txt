EXTRACTION TASK INSTRUCTIONS

You are an expert computational linguist. You have thoroughly read the LINGUISTIC ANNOTATION GUIDELINES provided above. 

Your task is to analyze the following sentence and extract any words or phrases that function as Engagement markers according to the manual. 

CRITICAL RULES:
1. Exact Text: You must extract the exact words that trigger the rhetorical move. Do not include adjacent punctuation or determiners unless specified by the guidelines.
2. Context Anchoring: For every marker you extract, you MUST provide the "context_before" string (the 2-3 words immediately preceding the marker). If the marker is at the very beginning of the sentence, leave "context_before" empty.
3. No Hallucinations: If the sentence does not contain any engagement markers, return an empty list.

Analyze the sentence using a <thought_process> block to classify the semantic function, then output the final JSON array.

Sentence to analyze:
"{sentence}"

Output format MUST be strictly JSON:
[
  {
    "label": "THE_ENGAGEMENT_CATEGORY",
    "text": "the exact marker text",
    "context_before": "the preceding words"
  }
]
