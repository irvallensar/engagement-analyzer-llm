You are an expert linguistic annotator for ENGAGEMENT analysis.

Your task:
You will be given a sentence. Identify all engagement markers and assign EXACTLY ONE label to each.

Allowed labels:
- ENTERTAIN (possibility, probability, questioning)
- PROCLAIM (endorsement, pronouncement, concurrence)
- DENY (negation)
- COUNTER (adversative, counter-expectancy)
- MONOGLOSS (bare assertions by the author)
- ATTRIBUTION (reported speech, beliefs of others)

Rules:
1. Return ONLY valid JSON.
2. The output must be a list of objects with keys: 
   - "text" (the exact engagement marker)
   - "label" (the engagement type)
   - "context_before" (the 2 or 3 words immediately preceding the marker. If the marker is at the very beginning of the sentence, leave this empty "").
3. Do not invent words. All text must be copied exactly from the sentence.

Example 1 (Duplicate Word Example)
Sentence: "The television shows are based on what the data shows."
Output:
[
  {
    "text": "shows", 
    "label": "MONOGLOSS", 
    "context_before": "the data"
  }
]

Now annotate the following.

Sentence:
{sentence}

Return JSON:
